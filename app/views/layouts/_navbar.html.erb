<nav class="navbar navbar-expand-lg bg-body-tertiary bg-light">
  <div class="container-fluid">
    <%= link_to "Revue", root_path, class: "nav-link text-primary fw-bolder"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="mx-auto navbar-nav mb-2 mb-lg-0">       
          <%= link_to "Home", root_path, class: "fw-bold nav-link"%>
          <%= link_to "Albums", albums_path, class: "fw-bold nav-link"%>          
      </div>      
      <div class="navbar-nav mb-2 mb-lg-0 gap-3">
          <% if user_signed_in? %>
            <%= link_to "Edit profile", edit_user_registration_path, class: "fw-bold btn btn-outline-success" %>
            <div> Welcome <%= current_user.username %> </div> 
              <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "fw-bold rounded btn btn-outline-primary" %> 
            <% else %>
              <%= link_to "Sign up", new_user_registration_path, data: { turbo_method: :get }, class: "fw-bold btn btn-outline-secondary" %> 
              <%= link_to "Sign in", new_user_session_path, class: "fw-bold btn btn-outline-primary" %> 
            <% end %>
      </div>      
    </div>
  </div>
</nav>